// Objetos Math
// window.math patra ver las funciones de math


let resultado;

resultado = Math.PI; // devuelve el valor de pi
Math.random // genera un numero aleatorio entre 0 y 1
resultado = Math.ceil(2.4); // redondea hacia arriba
resultado = Math.floor(2.4); // redondea hacia abajo)
resultado = Math.sqrt(64); // devuelve la raiz cuadrada
resultado = Math.abs(-200); // devuelve el valor a positivo
resultado = Math.pow(8, 2); // devuelve el resultado de 8 elevado a 2
resultado = Math.max(2, 3, 4, 5, 6); // devuelve el numero mayor
resultado = Math.min(2, 3, 4, 5, 6); // devuelve el numero menor
resultado = Math.random() * 100; // genera un numero aleatorio entre 0 y 100
resultado = Math.floor(Math.random() * 100); // genera un numero aleatorio entre 0 y 100 y lo redondea hacia abajo entero
console.log(resultado);

